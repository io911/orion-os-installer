# Orion OS Installer

The ISO produced by the workflow includes the exact packages configured by Orion OS.

Notes:
- Installer is interactive: users set their own username/password and partitioning during installation.
- Once installed, just update via terminal for security and package updates: "sudo bootc update"
